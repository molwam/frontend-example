import * as ProgramTypes from './Types';

export const Spain: ProgramTypes.Country = {
    id: 'ES',
    label: 'Spain'
};

export const Germany: ProgramTypes.Country = {
    id: 'GER',
    label: 'Germany'
};
